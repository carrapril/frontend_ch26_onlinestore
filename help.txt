run server: 
npm start

git commands: 
(1 time) initiatlize repo:
git init

add the remote url:
git remote add origin <url>

adding files:
git add .

local copy:
git commit -m "message"

upload to server:
git push -u origin master

(nth time):
git push

check status
git status
